APP_NAME="Adoption Advisory"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://127.0.0.1:8000

LOG_CHANNEL=stack

# No database — all client data lives in the browser (localStorage), exported via Excel.
SESSION_DRIVER=cookie
CACHE_STORE=file
QUEUE_CONNECTION=sync
MAIL_MAILER=log
